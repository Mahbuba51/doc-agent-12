# Stage 0 — problem statement (one per group). Set by assignment.
group_id: "TBD"   # team id not yet assigned by the supervisor — update once known
domain: "Land Registry Archives & Legal Aid"
data_speciality: "bangla-english-codeswitch-handwritten"   # cursive Bangla HTR + inline English/Bangla-numeral code-switching
primary_nfr: "precision-first"
nfr_target: "answer-F1 >= 0.92 and critical-field Exact Match >= 95% (plot no., deed amount, dates, party names)"
corpus:
  min_pages: 300
  min_words: 60000
  source_url: "https://data.mendeley.com/datasets/yk3c3xy9vm/1"   # Bangla Handwritten Dolil Dataset (653 Images), DOI 10.17632/yk3c3xy9vm.1
  license: "CC BY 4.0 (re-share permitted with attribution)"
